{
  "name": "phrasee_api_test",
  "version": "1.0.0",
  "description": "PHRASEE BACK END TECHNICAL TEST",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests think TDD would have taken me a bit longer and showed a bit less skills \""
  },
  "dependencies": {},
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "typescript": "^4.0.0"
  }
}
